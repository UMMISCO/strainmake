# database locations are set in the StrainMake config file (`databases:` section)
name: meteor
channels:
  - bioconda
  - conda-forge
dependencies:
  - python=3.12
  - bowtie2>=2.3.5
  - pip
  - pip:
    - meteor==2.0.21
